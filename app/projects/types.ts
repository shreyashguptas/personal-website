export interface Project {
    id: string
    title: string
    url?: string
    tags: string[]
    date: string
    details: string
    pinned?: boolean
    image?: string
    created_at: string
    updated_at: string
}
  
  